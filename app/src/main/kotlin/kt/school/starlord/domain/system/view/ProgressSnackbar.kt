package kt.school.starlord.domain.system.view

/**
 * Controls displaying snackbar with progress text.
 */
interface ProgressSnackbar {

    /**
     * Apply`s visibility for snackbar.
     */
    fun setVisibility(visible: Boolean)
}
